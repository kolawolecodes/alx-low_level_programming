#include "main.h"

/**
 * _isupper - evaluate if letter is uppercase
 * @c: pringt int
 * Return: Always 0.
 */
int _isupper(int c)

{

	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}

	else
	{
		return (0);
	}

}
